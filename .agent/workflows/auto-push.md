---
description: Sincronizar automáticamente todos los cambios con GitHub
---

Este workflow automatiza el proceso de subir los cambios al repositorio remoto.

1. Añadir todos los cambios
// turbo
`git add .`

2. Crear un commit con los cambios
// turbo
`git commit -m "Sincronización automática de cambios"`

3. Subir a la rama main
// turbo
`git push origin main`
